# PostgreSQL for production, SQLite is fine for local dev (see prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Auth (Phase 8) - generate with: openssl rand -base64 32
AUTH_SECRET="replace-me"

# Optional: base URL used by future audio/reciter integrations (Phase 7)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
